import type { TabProps } from "../SettingsHelpers";

export default function GeneralTab(_props: TabProps) {
  return (
    <div className="space-y-4">
      <p className="text-sm text-ink-muted">No general settings available.</p>
    </div>
  );
}
